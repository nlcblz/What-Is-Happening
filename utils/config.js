// utils/config.js
// 前端配置

module.exports = {
  // 后端 API 地址
  // 本地开发时使用 localhost，部署后替换为云托管地址
  apiBaseUrl: 'http://localhost:3000',

  // 默认分页大小
  pageSize: 20,

  // AI 配置默认值
  defaultAiProvider: 'deepseek'
}
